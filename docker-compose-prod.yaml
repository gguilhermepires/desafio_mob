version: "3.9"
services:
  autenticacao-api:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
        - .env
    entrypoint: ['yarn', 'dev']
    network_mode: host

